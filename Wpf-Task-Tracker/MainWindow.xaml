<Window x:Class="TeamNorthStar_TaskTrackerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TeamNorthStar_TaskTrackerApp.Models"
        Title="Task Tracker" Height="450" Width="800">
    <Grid>
<Grid.Background>
    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="Black"/>
        <GradientStop Color="DeepSkyBlue" Offset="1"/>
    </LinearGradientBrush>
</Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Task Input Section -->
        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
            <TextBox Name="TitleInput" Width="150"
                     GotFocus="ClearPlaceholder" LostFocus="SetPlaceholder">
                Enter task title...
            </TextBox>
            <TextBox Name="DescriptionInput" Width="250"
                     GotFocus="ClearPlaceholder" LostFocus="SetPlaceholder">
                Enter task description...
            </TextBox>
            <DatePicker Name="DueDatePicker" Width="150"/>
            <ComboBox Name="StatusDropdown" Width="150">
                <ComboBoxItem Content="NotStarted" IsSelected="True"/>
                <ComboBoxItem Content="InProgress"/>
                <ComboBoxItem Content="Completed"/>
            </ComboBox>
            <Button Content="Add Task" Click="OnAddTaskButtonClick"/>
            <Button Content="Export Tasks" Click="OnExportButtonClick" Margin="5"/>
            <Button Content="Import Tasks" Click="OnImportButtonClick" Margin="5"/>
        </StackPanel>

        <!-- Task List Section -->
        <ListView ItemsSource="{Binding Tasks}" Grid.Row="1" Margin="10">
            <ListView.Background>
    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="LightGray"/>
        <GradientStop Color="#FFFF5050" Offset="1"/>
    </LinearGradientBrush>
</ListView.Background>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}" />
                    <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" />
                    <GridViewColumn Header="Due Date" DisplayMemberBinding="{Binding DueDate}" />
                    <GridViewColumn Header="Status">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox SelectedItem="{Binding Status, Mode=TwoWay}" 
                                          ItemsSource="{x:Static local:TaskStatusHelper.Values}" 
                                          SelectionChanged="OnStatusChanged" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>

